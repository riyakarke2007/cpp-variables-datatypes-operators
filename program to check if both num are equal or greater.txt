//Write a program to input two number and check if both are equal or first is greater or second is greater.
#include<iostream>
using namespace std;
int main()
{
    int a,b;
    cout<<"Enter any two numbers:";
    cin>>a>>b;
    if(a==b){
        cout<<"Entered numbers are equal";
    }
        else if(a>=b){
            cout<<"First number is greater than second number:";
        }else{
            cout<<"second number is greater than first number";
        }
        return 0;
    }
